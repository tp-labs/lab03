language: cpp
compiler: clang

os: linux
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - clang-6.0
script:
- export CXX=clang++-6.0
- cmake --version
- cmake CMakeLists.txt
- cmake --build . --target example_s
- cmake --build . --target example_h
